import React from "react";
import { Link } from "react-router-dom";
import logo from "../logo.svg";

const Header = (props) => (
    <header className="App-header">
        <h1>Task It Application</h1>
    </header>
);

export default Header;